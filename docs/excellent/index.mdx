---
title: 精选篇
---

# {frontmatter.title}

:::tip
有赞一面
:::

## 01 `CSS`选择器以及选择器的优先级？

## 02 什么是`BFC`？

## 03 触发`BFC`的条件是什么？

## 04 `BFC`的约束规则是什么？

## 05 `BFC`可以解决的问题？

## 06 盒模型的理解？

## 07 `css`如何实现左侧宽度固定，右侧宽度自适应的布局？
### 方案1 利用`float`+`margin`
### 方案2 利用`calc`计算宽度
### 方案3 利用`float`+`overflow`实现
### 方案4 利用flex布局实现

## 08 了没跨域吗？一般什么情况下会导致跨域？

## 09 导致跨域行为有哪些？

## 10 `JSONP`的实现原理？

## 11 如何实现一个`jsonp`?

## 12 `jsonp`的安全性问题？

## 13 `cors`跨域资源共享的原理？

## 14 `jsonp`与`cors`的对比？

## 15 其他跨域解决方案？

## 16 `HTTP2`与`HTTP1`有什么区别？

## 17 你能说说缓存吗？

## 18 首频加载优化有哪些方案？

## 19 如何在`Node`端配置路径别名【类似于`Webpack`中的alias配置】?

## 20 对作用域链的理解？

## 21 Vue响应式原理？

## 22 了解`Eventloop`吗？

## 23 如何避免重绘与回流？

:::tip
有赞2面
:::

## 24 浏览器事件循环的原理？
> 考察点：
> - 宏任务
> - 微任务、消息队列

```js
messageQueue;
```

## 25 宏任务和微任务如何划分？
> 浏览器的宏任务、微任务、`node`中的宏任务、微任务
### 宏任务
- `script`
- `setTimeout`
- `setInterval`
### 微任务
- `Promise.then`

## 26 如何判断 js 变量类型？
> js 底层数据类型的判断
```js
  String
  Number
  Boolean
  null
  undefined
  Symbol
  Object
  // 数据类型的检测方法
  typeof
  instanceof
  Array.isArray
  Object.prototype.toString.call(value)
```

## 27 说一下`vdom`和`diff`?
虚拟dom与diff算法

## 28 `diff`算法的时间复杂度？
```js
数据结构之间的比较方式
```

## 29 `hooks`相对于之前的开发方式有什么特点？
在开发中比较常用的hooks
```js title="hooks"
useState 状态管理
useReducer 处理较多的逻辑
useEffect 副作用
useCallback 优化
useMemo 优化
useDispatch redux
useSelector redux
useContext
```

## 30 `React`开发中有哪些性能优化的方式？

## 31 `setState`是异步的吗？
可能是同步、也可能是异步

## 32 如何实现批量更新的策略？

## 33 `HTPP2`和`HTTP1`的优化？

:::tip
滴滴面经
:::

## 34 你知道有哪些安全问题？如何避免？
CSRF/XSRF

## 35 介绍一下`Graphql`?

## 36 说说`Vue`中`$nexttick`的实现原理？

## 37 如何实现居中？

## 38 使用过flex吗？说下具体原理

## 39 bind的源码实现？

## 40 伪类与伪元素的区别？

:::tip
51信用卡一面
:::

## 41 在`ES5`中如何实现继承？

## 42 怎么理解定位？

## 43 防抖与节流的原理是什么？具体的使用场景？

## 44 Vue中computed实现原理？

:::tip
阿里部门1面
:::
## 45 谈谈`Webpack`的实现原理？

## 46 首屏优化有哪些解决方案？

## 47 `Node.js`的加载机制[require和module.exports]
```js
const path = require('path');

moduel.export = {
  foo,
  bar,
};
```

## 48 你觉得你最擅长什么？

## 49 `React`与`Vue`的区别？

:::tip
阿里CBU技术部一面
:::

## 50 说说`z-index`有什么需要注意的地方？

## 51 熟悉`CSS3`动画吗？

## 52 有没有做过什么可视化的项目？

## 53 你觉得你最擅长什么？

## 54 `Flex`实现两列布局？

## 55 说说`DOM`事件流？

## 56 你觉得你有做过推动流程或者改善流程的事件吗？举例说明？

:::tip
阿里企业职能事业部一面
:::

## 57 webpack的loader与plugins的区别？

## 58 HTTP状态码206是做什么？

## 59 React高阶组件有哪些？

## 60 Service Worker有哪些作用？

## 61 跨域的解决方案有哪些？

:::tip
阿里企业职能事业部二面
:::

## 62 Vue的整个实现原理？

## 63 Chrome插件如何屏蔽广告？

## 64 如何判断两个变量是否相等？

## 65 Vue中数据为什么频繁变化但只会更新一次？

## 66 绝对定位、固定定位与`z-index`

:::tip
网易面经
:::

## 67 H5新特性有哪些？实际的使用场景有哪些？

## 68 Vue组件之间通信？

## 69 遍历对象方法？

## 70 数组去重？

## 71 数组的扁平化？

## 72 深度遍历？

## 73 链表的相加问题？

## 74 `Promise`的具体原理？

## 75 聊一聊`Map`与`Set`?

## 76 前端性能优化？
